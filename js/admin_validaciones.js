$(function(){ 
/*
 jQuery.validator.addMethod(
 	"customFormat", 
 	function(value, element){
        return value.match(/^(0?[1-9]|[12][0-9]|3[0-1])[/., -](0?[1-9]|1[0-2])[/., -](19|20)?\d{2}$/);
    },
    "Ingresa la fecha en formato dd/mm/yyyy"
 );

 $("#AdministracionEventos").validate({
 	lang: 'es',
	rules: {
		id_evento: {required: true, maxlength:15},
		nombre: {required: true, maxlength:255},
		f_inicial:{required:true, customFormat:true, maxlength:10},
		f_final:{required:true, customFormat:true, maxlength:10}
	},
	messages: {
		id_evento:{number:"Sólo números", maxlength:"Máximo 10 caracteres"},
		nombre: {maxlength:"Máximo 255 caracteres"},
		f_inicial:{maxlength:"Máximo 10 caracteres"},
		f_final:{maxlength:"Máximo 10 caracteres"}
	},
	tooltip_options: {
		id_evento: {trigger:'focus'}
	},
	focusInvalid: false,
    invalidHandler: function(form, validator) {

        if (!validator.numberOfInvalids())
            return;

        $('html, body').animate({
            scrollTop: $(validator.errorList[0].element).offset().top
        }, 500);

    }
 });
*/
});